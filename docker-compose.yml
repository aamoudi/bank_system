version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:26817"
    environment:
      - APP_NAME=MASAREEFI
      - APP_ENV=local
      - APP_KEY=base64:l/3YDJNfb2DP9InslzPtIyVyJyTpO6sahq/gAGIIxKQ=
      - APP_DEBUG=true
      - APP_URL=http://localhost:8080
      - DB_CONNECTION=mysql
      - DB_HOST=mysql-72244cf-masareefi.l.aivencloud.com
      - DB_PORT=26817
      - DB_DATABASE=defaultdb
      - DB_USERNAME=avnadmin
      - DB_PASSWORD=
      - DB_SSLMODE=require
      - MYSQL_ATTR_SSL_CA=/var/www/html/docker/aiven-ca.pem
      - CACHE_DRIVER=file
      - SESSION_DRIVER=file
    volumes:
      - ./storage:/var/www/html/storage
      - ./docker/aiven-ca.pem:/var/www/html/docker/aiven-ca.pem